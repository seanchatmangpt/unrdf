# Part 4: Process Lifecycle and Supervision

Crashes become explicit lifecycle transitions. Supervisors restart fresh state, intensity windows bound crash loops, startup requires acknowledgement, links express shared fate, monitors observe, and retry scopes isolate attempts.

Every example in this part is executed by the Chicago suite and has a corresponding real AtomVM marker.
