# Part 1: Shape Your Data

Frozen messages, closed tagged protocols, immutable state, railway results, and validated domain values form the base layer. No process is introduced until the data is lawful by construction.

Every example in this part is executed by the Chicago suite and has a corresponding real AtomVM marker.
