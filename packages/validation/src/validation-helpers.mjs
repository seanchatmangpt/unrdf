/**
 * @file Validation Helpers
 * @module validation/validation-helpers
 *
 * @description
 * Helper utilities for validation runner.
 * Currently a stub - functionality moved to other modules.
 */

/**
 * Create validation helpers
 * @returns {Object} Validation helpers
 */
export function createValidationHelpers() {
  return {
    // Placeholder for future helper methods
    // Currently all helpers are integrated into ValidationRunner
  };
}

/**
 * Default validation helpers instance
 */
export const defaultValidationHelpers = createValidationHelpers();
