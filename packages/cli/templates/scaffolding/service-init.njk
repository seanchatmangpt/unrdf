---
to: '{{ service_name | default("service") | lower }}/README.md'
description: "Initialize microservice with standard structure"
category: scaffolding
requires:
  - service_name
mode: overwrite
---

# {{ service_name | default("codemanufactory-service") }}

> Microservice scaffolded from CodeManufactory ontology

## Overview

{{ service_name | default("codemanufactory-service") }} is a microservice built for high-performance and scalability.

## Quick Start

```bash
cd {{ service_name | lower | default("service") }}
make dev
make test
make build
```

## Service Details

- **Name**: {{ service_name | default("codemanufactory-service") }}
- **Port**: {{ port | default("8080") }}

## API Endpoints

### Health Check

```
GET /health
```

**Response:**
```json
{
  "status": "healthy",
  "service": "{{ service_name | lower | default("service") }}",
  "version": "1.0.0"
}
```

## Configuration

### Environment Variables

```bash
SERVICE_NAME={{ service_name | upper | default("SERVICE") }}
SERVICE_PORT={{ port | default("8080") }}
NODE_ENV=development
OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4317
```

## Deployment

### Docker

```bash
docker build -t {{ service_name | lower | default("service") }}:latest .
docker run -p {{ port | default("8080") }}:{{ port | default("8080") }} {{ service_name | lower | default("service") }}:latest
```

## Monitoring

- **Health Endpoint**: `http://localhost:{{ port | default("8080") }}/health`
- **Metrics**: `http://localhost:{{ port | default("8080") }}/metrics`
- **Traces**: Available via OpenTelemetry
