// DO NOT EDIT — generated by `weaver registry generate`
// Source: otel/registry/
// Re-generate: cd otel && weaver registry generate --registry registry/ js ../packages/otel/src/generated/
//
// Semantic convention attribute name constants for UNRDF.
// Import these instead of using raw strings to catch typos at lint time.


// ── Groq LLM Attributes ──

/** The API endpoint URL being called. */
export const ATTR_GROQ_API_ENDPOINT = 'groq.api.endpoint';

/** The Groq model identifier being used for generation. */
export const ATTR_GROQ_API_MODEL = 'groq.api.model';

/** The API provider hosting the model. */
export const ATTR_GROQ_API_PROVIDER = 'groq.api.provider';

/** Maximum tokens requested in the generation. */
export const ATTR_GROQ_GENERATION_MAX_TOKENS = 'groq.generation.max_tokens';

/** Length in characters of the prompt sent to the LLM. */
export const ATTR_GROQ_GENERATION_PROMPT_LENGTH = 'groq.generation.prompt_length';

/** Length in characters of the LLM response content. */
export const ATTR_GROQ_GENERATION_RESPONSE_LENGTH = 'groq.generation.response_length';

/** Temperature parameter used for generation (controls randomness). */
export const ATTR_GROQ_GENERATION_TEMPERATURE = 'groq.generation.temperature';

/** Number of RDF triples successfully generated and parsed. */
export const ATTR_GROQ_RDF_TRIPLES_GENERATED = 'groq.rdf.triples_generated';

/** Whether the LLM response could be parsed as valid RDF triples. */
export const ATTR_GROQ_RDF_TRIPLES_PARSE_ERROR = 'groq.rdf.triples_parse_error';

/** Duration in milliseconds of the HTTP request to Groq API. */
export const ATTR_GROQ_RESPONSE_DURATION_MS = 'groq.response.duration_ms';

/** HTTP status code returned by the Groq API. */
export const ATTR_GROQ_RESPONSE_STATUS_CODE = 'groq.response.status_code';



// ── MCP (Model Context Protocol) Attributes ──

/** The name of the MCP server handling the request. */
export const ATTR_MCP_SERVER_NAME = 'mcp.server.name';

/** JSON-serialized arguments passed to the MCP tool. May be truncated for large payloads. */
export const ATTR_MCP_TOOL_ARGS = 'mcp.tool.args';

/** The class or type of error that caused the tool invocation to fail. Follows the same conventions as `error.type`. */
export const ATTR_MCP_TOOL_ERROR_TYPE = 'mcp.tool.error_type';

/** The name of the MCP tool being invoked. */
export const ATTR_MCP_TOOL_NAME = 'mcp.tool.name';

/** Size in bytes of the JSON-serialized result returned by the MCP tool. */
export const ATTR_MCP_TOOL_RESULT_SIZE = 'mcp.tool.result_size';

/** Whether the MCP tool invocation completed without error. */
export const ATTR_MCP_TOOL_SUCCESS = 'mcp.tool.success';



// ── POWL Process Mining ──

/** PM4Py POWL discovery algorithm variant used. */
export const ATTR_POWL_DISCOVERY_VARIANT = 'powl.discovery.variant';

/** Category of error detected during POWL generation: schema validation failure, semantic constraint violation, or conformance threshold breach. */
export const ATTR_POWL_ERROR_CATEGORY = 'powl.error_category';

/** Number of LLM generation and repair iterations performed to reach the final POWL model. */
export const ATTR_POWL_ITERATION_COUNT = 'powl.iteration.count';

/** Conformance fitness score (0.0–1.0) of the POWL model against the event log. */
export const ATTR_POWL_MODEL_FITNESS = 'powl.model.fitness';

/** POWL operator types present in the discovered model (xor, loop, partial_order). */
export const ATTR_POWL_MODEL_OPERATOR_TYPES = 'powl.model.operator_types';

/** URL-safe slug identifying the POWL process model. */
export const ATTR_POWL_MODEL_SLUG = 'powl.model.slug';

/** Current repair iteration number (1-based). Present only during error repair sub-loops. */
export const ATTR_POWL_REPAIR_ITERATION = 'powl.repair_iteration';

/** Whether the generated POWL model met the configured minimum fitness threshold. */
export const ATTR_POWL_THRESHOLD_MET = 'powl.threshold_met';



// ── POWL Swarm Coordination ──

/** IRI of the ExecutionClaim lease record. */
export const ATTR_SWARM_CLAIM_IRI = 'swarm.claim.iri';

/** Lease duration in milliseconds. */
export const ATTR_SWARM_CLAIM_LEASE_MS = 'swarm.claim.lease_ms';

/** Fraction of workflow tasks completed (0.0–1.0). */
export const ATTR_SWARM_COMPLETION_PCT = 'swarm.completion.pct';

/** Failure handling policy for a stalled or failed task. */
export const ATTR_SWARM_FAILURE_POLICY = 'swarm.failure.policy';

/** IRI of the POWL WorkflowSpecification being executed. */
export const ATTR_SWARM_SPEC_IRI = 'swarm.spec.iri';

/** Human-readable activity label from powl:activityLabel. */
export const ATTR_SWARM_TASK_LABEL = 'swarm.task.label';

/** IRI of the POWL activity node being executed by this worker. */
export const ATTR_SWARM_TASK_NODE_IRI = 'swarm.task.node_iri';

/** POWL node type of the task. */
export const ATTR_SWARM_TASK_NODE_TYPE = 'swarm.task.node_type';

/** Number of tasks with status=completed. */
export const ATTR_SWARM_TASKS_COMPLETED = 'swarm.tasks.completed';

/** Number of tasks with status=failed. */
export const ATTR_SWARM_TASKS_FAILED = 'swarm.tasks.failed';

/** Number of tasks with expired leases (claimed or executing but claimedUntil < now). */
export const ATTR_SWARM_TASKS_STALLED = 'swarm.tasks.stalled';

/** Total number of tasks in the workflow spec. */
export const ATTR_SWARM_TASKS_TOTAL = 'swarm.tasks.total';

/** Number of currently active workers in the swarm. */
export const ATTR_SWARM_WORKER_COUNT = 'swarm.worker.count';

/** Unique worker identifier within the swarm. */
export const ATTR_SWARM_WORKER_ID = 'swarm.worker.id';



// ── UNRDF CLI Attributes ──

/** Number of positional and option arguments passed to the command. */
export const ATTR_UNRDF_CLI_ARGS_COUNT = 'unrdf.cli.args_count';

/** The top-level CLI command being executed. */
export const ATTR_UNRDF_CLI_COMMAND = 'unrdf.cli.command';

/** Path to the JSON configuration file passed via `--config`. */
export const ATTR_UNRDF_CLI_CONFIG_FILE = 'unrdf.cli.config_file';

/** The process exit code produced by the CLI command. */
export const ATTR_UNRDF_CLI_EXIT_CODE = 'unrdf.cli.exit_code';

/** The output format requested via `--format` or `--output`. */
export const ATTR_UNRDF_CLI_OUTPUT_FORMAT = 'unrdf.cli.output_format';

/** Path to the RDF store file passed via `--store`. Recorded as-is; not normalized or resolved to an absolute path. */
export const ATTR_UNRDF_CLI_STORE_FILE = 'unrdf.cli.store_file';

/** The subcommand being executed within the top-level CLI command. Empty string if no subcommand was provided. */
export const ATTR_UNRDF_CLI_SUBCOMMAND = 'unrdf.cli.subcommand';



// ── UNRDF Cryptographic Provenance Attributes ──

/** The cryptographic algorithm used for the operation. */
export const ATTR_UNRDF_CRYPTO_ALGORITHM = 'unrdf.crypto.algorithm';

/** Number of receipts in the verified chain. */
export const ATTR_UNRDF_CRYPTO_CHAIN_LENGTH = 'unrdf.crypto.chain_length';

/** The cryptographic operation being performed. */
export const ATTR_UNRDF_CRYPTO_OPERATION = 'unrdf.crypto.operation';

/** The UUID identifier of the cryptographic receipt produced or verified. */
export const ATTR_UNRDF_CRYPTO_RECEIPT_ID = 'unrdf.crypto.receipt_id';

/** The result of a cryptographic verification operation. */
export const ATTR_UNRDF_CRYPTO_VERIFICATION_RESULT = 'unrdf.crypto.verification_result';



// ── UNRDF Knowledge Hook Attributes ──

/** The kind of condition evaluated by this hook. */
export const ATTR_UNRDF_HOOK_CONDITION_KIND = 'unrdf.hook.condition_kind';

/** The kind of effect executed when the hook condition is satisfied. */
export const ATTR_UNRDF_HOOK_EFFECT_KIND = 'unrdf.hook.effect_kind';

/** Whether the hook was enabled at evaluation time. */
export const ATTR_UNRDF_HOOK_ENABLED = 'unrdf.hook.enabled';

/** Number of hooks whose conditions evaluated to satisfied in a batch. */
export const ATTR_UNRDF_HOOK_HOOKS_SATISFIED = 'unrdf.hook.hooks_satisfied';

/** Total number of hooks evaluated in a batch execution. */
export const ATTR_UNRDF_HOOK_HOOKS_TOTAL = 'unrdf.hook.hooks_total';

/** The UUID identifier of the knowledge hook being evaluated. */
export const ATTR_UNRDF_HOOK_ID = 'unrdf.hook.id';

/** The human-readable name of the knowledge hook. */
export const ATTR_UNRDF_HOOK_NAME = 'unrdf.hook.name';

/** The execution priority of the hook. Lower values execute first. Range: 0–100; default is 50. */
export const ATTR_UNRDF_HOOK_PRIORITY = 'unrdf.hook.priority';

/** Number of RDF quads added to the store as a result of executing a `sparql-construct` effect. */
export const ATTR_UNRDF_HOOK_QUADS_ADDED = 'unrdf.hook.quads_added';

/** Whether the hook condition evaluated to satisfied (true). */
export const ATTR_UNRDF_HOOK_SATISFIED = 'unrdf.hook.satisfied';



// ── UNRDF Policy Attributes ──

/** Number of policy hooks registered in the registry. */
export const ATTR_UNRDF_POLICY_HOOKS_REGISTERED = 'unrdf.policy.hooks_registered';

/** The unique identifier of the policy pack being applied. */
export const ATTR_UNRDF_POLICY_ID = 'unrdf.policy.id';

/** Human-readable display name of the policy pack. */
export const ATTR_UNRDF_POLICY_NAME = 'unrdf.policy.name';

/** The outcome of applying the policy to a quad. */
export const ATTR_UNRDF_POLICY_RESULT = 'unrdf.policy.result';

/** The lifecycle trigger point that activates the policy hook. */
export const ATTR_UNRDF_POLICY_TRIGGER = 'unrdf.policy.trigger';



// ── UNRDF RDF Operation Attributes ──

/** MIME type of the RDF serialization format used for load or dump. */
export const ATTR_UNRDF_RDF_FORMAT = 'unrdf.rdf.format';

/** The IRI of the named graph being operated on. Empty string or absent for the default graph. */
export const ATTR_UNRDF_RDF_GRAPH_NAME = 'unrdf.rdf.graph_name';

/** The type of RDF store operation being performed. */
export const ATTR_UNRDF_RDF_OPERATION = 'unrdf.rdf.operation';

/** Number of RDF quads involved in the operation (added, deleted, or returned by a query). */
export const ATTR_UNRDF_RDF_QUAD_COUNT = 'unrdf.rdf.quad_count';

/** Length in characters of the SPARQL query string. Useful for distinguishing simple vs. complex queries in latency analysis. */
export const ATTR_UNRDF_RDF_SPARQL_QUERY_LENGTH = 'unrdf.rdf.sparql_query_length';

/** The storage backend used by the RDF store. */
export const ATTR_UNRDF_RDF_STORE_BACKEND = 'unrdf.rdf.store_backend';



// ── UNRDF Transaction Attributes ──

/** The UUID identifier of the transaction. */
export const ATTR_UNRDF_TRANSACTION_ID = 'unrdf.transaction.id';

/** The isolation level of the transaction. */
export const ATTR_UNRDF_TRANSACTION_ISOLATION_LEVEL = 'unrdf.transaction.isolation_level';

/** Number of RDF operations (add, delete, update) performed within the transaction before commit or rollback. */
export const ATTR_UNRDF_TRANSACTION_OPERATION_COUNT = 'unrdf.transaction.operation_count';

/** The outcome of the transaction. */
export const ATTR_UNRDF_TRANSACTION_RESULT = 'unrdf.transaction.result';


