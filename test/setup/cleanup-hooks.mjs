/**
 * Global test cleanup hooks
 * Runs after each test to clean up resources
 */

import { afterEach } from 'vitest'

/**
 * Cleanup after each test
 */
afterEach(() => {
  // Global cleanup handled in individual test files
})
