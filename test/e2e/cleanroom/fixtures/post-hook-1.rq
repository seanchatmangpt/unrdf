# Post-Hook 1: Verify consistency after transaction
PREFIX schema: <http://schema.org/>

ASK {
  # All persons who work for org must have job title
  NOT EXISTS {
    ?person schema:worksFor ?org .
    FILTER NOT EXISTS { ?person schema:jobTitle ?title }
  }
}
