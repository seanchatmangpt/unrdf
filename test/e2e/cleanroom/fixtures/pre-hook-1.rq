# Pre-Hook 1: Validate data types
PREFIX schema: <http://schema.org/>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>

ASK {
  # Check that all ages are integers
  NOT EXISTS {
    ?person schema:age ?age .
    FILTER (!isLiteral(?age) || datatype(?age) != xsd:integer)
  }
}
