{
  "name": "security-policy",
  "version": "1.0.0",
  "description": "Security policy for sensitive data protection",
  "policies": [
    {
      "id": "no-plain-passwords",
      "name": "No Plain Text Passwords",
      "description": "Prevents storage of plain text passwords",
      "enabled": true,
      "severity": "critical",
      "rules": [
        {
          "type": "sparql-ask",
          "query": "PREFIX schema: <http://schema.org/> ASK { NOT EXISTS { ?x schema:password ?pwd } }",
          "message": "Passwords must not be stored in plain text"
        }
      ]
    }
  ]
}
