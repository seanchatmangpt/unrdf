# @unrdf PhD Thesis Documentation

Generated via 10-agent swarm from `/home/user/unrdf` source.

## Overview

This thesis documents the @unrdf ecosystem (47 packages) as a compositional foundation for RDF processing. Documentation follows Knowledge Graph Compiler (KGC) substrate notation where every claim is testable through interfaces, test receipts, or provenance hashes.

**Key Properties**:
- No external web links (all references to local interfaces/receipts)
- PhD-level technical rigor (formal definitions, proofs, impossibility guards)
- Receipt-based verification (every claim tied to `npm test` output, coverage reports, or git commits)
- Compositional semantics (observables O, artifacts A, reconcilers μ, types Σ, merge operators Π/⊕)

## Build

### Prerequisites

- LaTeX distribution (TeXLive, MiKTeX, or MacTeX)
- `pdflatex` and `bibtex` in PATH

### Quick Build

```bash
cd /home/user/unrdf/thesis
pdflatex main.tex
pdflatex main.tex  # Second pass for cross-references
```

**Output**: `main.pdf` (PhD thesis document)

### Advanced Build (latexmk)

For automatic dependency resolution and multi-pass compilation:

```bash
cd /home/user/unrdf/thesis
latexmk -pdf main.tex
```

**Output**: `main.pdf` with complete cross-references, ToC, and indices.

### Clean Build Artifacts

```bash
cd /home/user/unrdf/thesis
latexmk -c          # Remove intermediate files (.aux, .log, .toc)
latexmk -C          # Remove all generated files including main.pdf
```

## Structure

```
thesis/
├── main.tex                    # Main document (book structure)
├── preamble.tex               # KGC notation macros + LaTeX config
├── README.md                  # This file
└── packages/                  # Package documentation (generated by agents 3-10)
    ├── index.json             # Machine-readable package inventory
    ├── index.tex              # Package table (auto-generated)
    ├── agent_3_packages.tex   # Packages 1-6 (foundation layer)
    ├── agent_4_packages.tex   # Packages 7-12 (abstraction layer)
    ├── agent_5_packages.tex   # Packages 13-18 (streaming layer)
    ├── agent_6_packages.tex   # Packages 19-24 (validation layer)
    ├── agent_7_packages.tex   # Packages 25-30 (compiler layer)
    ├── agent_8_packages.tex   # Packages 31-36 (application layer)
    ├── agent_9_packages.tex   # Packages 37-42 (domain-specific)
    └── agent_10_packages.tex  # Packages 43-47 (utilities)
```

## KGC Notation Reference

The thesis uses the following mathematical notation (defined in `preamble.tex`):

| Symbol | LaTeX Macro | Meaning |
|--------|-------------|---------|
| O | `\Oobs` | Observable substrate (data sources) |
| A | `\Aout` | Artifact output (stores, proofs) |
| μ | `\muRecon` | Reconciler function (O → A) |
| Σ | `\SigmaType` | Type/schema signature (Zod, JSDoc) |
| Π | `\PiMerge` | Sequential merge operator (composition) |
| ⊕ | `\oplusMerge` | Commutative merge (store union) |
| Γ | `\GammaGlue` | Glue constraints (dependencies, exports) |
| H | `\GuardH` | Guard (impossibility predicate) |
| Q | `\InvQ` | Invariant preservation |
| τ | `\tauEpoch` | Epoch (time, causality) |
| hash | `\ProvHash` | Provenance hash (content-addressing) |

## Package Metadata Table

Each package is documented with:

```latex
\begin{pkgmeta}
\hline
Property & Value \\
\hline
Package & \pkg{@unrdf/oxigraph} \\
Version & [VERSION] \\
Observable $\Oobs$ & RDF files, SPARQL endpoints \\
Artifact $\Aout$ & Oxigraph store $\store$ \\
Type $\SigmaType$ & RDFJS DatasetCore \\
Guards $\GuardH$ & No \texttt{from 'n3'} imports \\
Invariants $\InvQ$ & Pure functions, deterministic \\
Receipts $\receipt{\cdot}$ & \texttt{npm test} (100\% pass) \\
Provenance $\ProvHash$ & \texttt{e387d0a1} \\
\hline
\end{pkgmeta}
```

## Verification Protocol

Before claiming "documentation complete", verify:

1. **Build succeeds**: `pdflatex main.tex` exits 0
2. **No undefined references**: Check `.log` file for `??` markers
3. **All packages documented**: 47 packages across agents 3-10
4. **Receipts valid**: Test outputs in `validation/receipts/` match claims
5. **Provenance matches**: Git commit hashes in metadata match `git log`

```bash
# Run full verification
pdflatex main.tex 2>&1 | tee build.log
grep -E "Warning|Error|Undefined" build.log  # Should be empty
grep -c "begin{pkgmeta}" packages/*.tex      # Should be 47
```

## Agent Responsibilities

| Agent | Packages | Layer |
|-------|----------|-------|
| 1 (Orchestrator) | — | Infrastructure (this README, preamble, main.tex) |
| 2 (Index) | — | Package inventory (index.json, index.tex) |
| 3-10 (Documenters) | 1-47 | Package chapters (5-6 packages each) |

## Quality Criteria

Documentation is complete when:

- [ ] `pdflatex main.tex` succeeds (exit code 0)
- [ ] All 47 packages have `\begin{pkgmeta}` tables
- [ ] Every claim references a receipt (`\receipt{\cdot}`) or provenance (`\ProvHash`)
- [ ] No external URLs (all references to local `/home/user/unrdf` paths)
- [ ] Guards $\GuardH$ and invariants $\InvQ$ defined for each package
- [ ] Compositional semantics ($\Oobs$, $\Aout$, $\muRecon$, $\SigmaType$) documented
- [ ] PDF renders correctly (figures, tables, code listings)

## Adversarial PM Checklist

Before declaring "thesis complete":

- **Did you BUILD it?** Or just write LaTeX?
  - Run: `pdflatex main.tex && ls -lh main.pdf`
- **Can you PROVE completeness?** Or assume 47 packages covered?
  - Run: `grep -c "begin{pkgmeta}" packages/*.tex`
- **What BREAKS if claims are wrong?** Be specific.
  - Invalid receipts → reproducibility failure
  - Missing $\GuardH$ → forbidden pattern leakage
- **What's the EVIDENCE?** Show build log, PDF size, package count.

## License

Generated documentation inherits @unrdf ecosystem license (check root `package.json`).

## Build Timestamp

Generated: 2025-12-27 (epoch τ = thesis creation date)
