// DO NOT EDIT — generated by `weaver registry generate`
// Source: otel/registry/
// Re-generate: cd otel && weaver registry generate --registry registry/ js ../packages/otel/src/generated/
//
// Semantic convention metric name constants for UNRDF.

{% for group in ctx.groups | selectattr("type", "equalto", "metric") | sort(attribute="metric_name") %}
/** {{ group.brief | trim }} */
export const METRIC_{{ group.metric_name | upper | replace(".", "_") }} = '{{ group.metric_name }}';
{% endfor %}
