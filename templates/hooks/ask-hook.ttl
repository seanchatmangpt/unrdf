# SPARQL ASK Hook Template
# This template provides a basic SPARQL ASK query for Knowledge Hook conditions.
# ASK queries return a boolean (true/false) and are ideal for simple trigger conditions.
#
# Usage:
# 1. Replace the condition logic in the WHERE clause
# 2. Update the comment to describe what this hook detects
# 3. Calculate the SHA-256 hash of this file
# 4. Reference this file in your Knowledge Hook definition
#
# Example: Detect when a resource has a specific property

PREFIX ex: <http://example.org/>
PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

# TODO: Describe what condition this ASK query checks
# Example: "Returns true if any resource has ex:status with value 'pending'"
ASK WHERE {
  # TODO: Replace with your condition logic
  # Example patterns:

  # Pattern 1: Check if a specific triple exists
  ?subject ex:hasProperty ex:someValue .

  # Pattern 2: Check if a value exceeds a threshold
  # ?subject ex:amount ?amount .
  # FILTER(?amount > 1000)

  # Pattern 3: Check if a required property is missing
  # ?subject rdf:type ex:RequiredType .
  # FILTER NOT EXISTS { ?subject ex:requiredProperty ?value }

  # Pattern 4: Check for data quality issues
  # ?subject ex:email ?email .
  # FILTER(!REGEX(?email, "^[^@]+@[^@]+\\.[^@]+$"))
}
