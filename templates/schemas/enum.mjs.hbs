{{!--
  @file templates/schemas/enum.mjs.hbs
  @description Handlebars template for generating Zod enum schemas from TypeScript type literals
  @usage Called by schema-generator.mjs with context: { schema: { name, description, values } }
--}}
/**
 * {{schema.description}}
 * @type {z.ZodEnum<[{{#each schema.values}}'{{this}}'{{#unless @last}}, {{/unless}}{{/each}}]>}
 */
export const {{schema.name}}Schema = z.enum([{{#each schema.values}}'{{this}}'{{#unless @last}}, {{/unless}}{{/each}}])

/**
 * {{schema.description}}
 * @typedef {z.infer<typeof {{schema.name}}Schema>} {{schema.name}}
 */
