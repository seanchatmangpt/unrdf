# Knowledge Hooks Benchmark Architecture - Visual Diagrams

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                     Knowledge Hooks Benchmark System                        │
│                              (80/20 Design)                                 │
└─────────────────────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────────────────────┐
│                           BENCHMARK EXECUTION LAYER                           │
├───────────────────────────────────────────────────────────────────────────────┤
│                                                                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │ Registration │  │  Execution   │  │  Concurrent  │  │    Memory    │    │
│  │  Benchmark   │  │  Benchmark   │  │  Benchmark   │  │  Benchmark   │    │
│  │              │  │              │  │              │  │              │    │
│  │  p95 < 10ms  │  │  p95 < 50ms  │  │  >1000 ops/s │  │  < 5MB/hook  │    │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘    │
│         │                 │                 │                 │              │
│  ┌──────────────┐  ┌──────────────┐                                         │
│  │  Condition   │  │   Circuit    │                                         │
│  │  Benchmark   │  │   Breaker    │                                         │
│  │              │  │  Benchmark   │                                         │
│  │  >80% cache  │  │  <1% trip    │                                         │
│  └──────┬───────┘  └──────┬───────┘                                         │
│         │                 │                                                  │
│         └─────────┬───────┴────────────────────────┬─────────────────┐     │
│                   │                                 │                 │     │
└───────────────────┼─────────────────────────────────┼─────────────────┼─────┘
                    │                                 │                 │
                    ▼                                 ▼                 ▼
┌───────────────────────────────────────────────────────────────────────────────┐
│                        INSTRUMENTATION LAYER                                  │
├───────────────────────────────────────────────────────────────────────────────┤
│                                                                               │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                    OpenTelemetry Tracer                             │    │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐             │    │
│  │  │ Span Creator │→ │ Span Collector│→ │ Span Exporter│             │    │
│  │  │              │  │  (In-Memory)  │  │   (OTLP)     │             │    │
│  │  └──────────────┘  └──────────────┘  └──────────────┘             │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                                                               │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                         Metric Collectors                           │    │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐             │    │
│  │  │   Latency    │  │  Throughput  │  │    Memory    │             │    │
│  │  │   Tracker    │  │   Counter    │  │   Profiler   │             │    │
│  │  └──────────────┘  └──────────────┘  └──────────────┘             │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                                                               │
└───────────────────────────────────┬───────────────────────────────────────────┘
                                    │
                                    ▼
┌───────────────────────────────────────────────────────────────────────────────┐
│                        AGGREGATION LAYER                                      │
├───────────────────────────────────────────────────────────────────────────────┤
│                                                                               │
│  ┌──────────────────────────────────────────────────────────────────┐       │
│  │                  Metrics Aggregator                              │       │
│  │                                                                  │       │
│  │  Input: OTEL Spans                                              │       │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐               │       │
│  │  │  Filter    │→ │   Group    │→ │  Calculate │               │       │
│  │  │  by Suite  │  │  by Metric │  │ Percentiles│               │       │
│  │  └────────────┘  └────────────┘  └────────────┘               │       │
│  │  Output: Aggregated Metrics                                    │       │
│  └──────────────────────────────────────────────────────────────────┘       │
│                                                                               │
│  ┌──────────────────────────────────────────────────────────────────┐       │
│  │               Percentile Calculator                              │       │
│  │                                                                  │       │
│  │  Input: Array of durations                                      │       │
│  │  Algorithm: Sort + Index calculation                            │       │
│  │  Output: { p50, p95, p99, max }                                 │       │
│  └──────────────────────────────────────────────────────────────────┘       │
│                                                                               │
└───────────────────────────────┬───────────────────────────────────────────────┘
                                │
                                ▼
┌───────────────────────────────────────────────────────────────────────────────┐
│                          VALIDATION LAYER                                     │
├───────────────────────────────────────────────────────────────────────────────┤
│                                                                               │
│  ┌──────────────────────────────────────────────────────────────────┐       │
│  │                  Acceptance Criteria Validator                   │       │
│  │                                                                  │       │
│  │  ┌─────────────────┐    ┌─────────────────┐                    │       │
│  │  │ Compare Metrics │ →  │ Generate Report │                    │       │
│  │  │  vs. Targets    │    │  (Pass/Fail)    │                    │       │
│  │  └─────────────────┘    └─────────────────┘                    │       │
│  │                                                                  │       │
│  │  Examples:                                                       │       │
│  │    p95 < 50ms?     ✅ (actual: 34.2ms)                          │       │
│  │    throughput > 1000? ✅ (actual: 1234 ops/s)                   │       │
│  │    memory < 5MB?   ✅ (actual: 4.8MB)                           │       │
│  └──────────────────────────────────────────────────────────────────┘       │
│                                                                               │
│  ┌──────────────────────────────────────────────────────────────────┐       │
│  │                Regression Detector                               │       │
│  │                                                                  │       │
│  │  Input: Current metrics + Baseline metrics                      │       │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐               │       │
│  │  │  Compare   │→ │  Calculate │→ │   Flag     │               │       │
│  │  │   Deltas   │  │   % Change │  │ Regressions│               │       │
│  │  └────────────┘  └────────────┘  └────────────┘               │       │
│  │                                                                  │       │
│  │  Thresholds:                                                     │       │
│  │    Latency: +20% → ❌ Block CI                                  │       │
│  │    Throughput: -15% → ❌ Block CI                               │       │
│  │    Memory: +30% → ❌ Block CI                                   │       │
│  └──────────────────────────────────────────────────────────────────┘       │
│                                                                               │
└───────────────────────────────┬───────────────────────────────────────────────┘
                                │
                                ▼
┌───────────────────────────────────────────────────────────────────────────────┐
│                          REPORTING LAYER                                      │
├───────────────────────────────────────────────────────────────────────────────┤
│                                                                               │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐          │
│  │   Console Report │  │   JSON Report    │  │  GitHub PR       │          │
│  │   (Human)        │  │   (Machine)      │  │  Comment         │          │
│  └──────────────────┘  └──────────────────┘  └──────────────────┘          │
│                                                                               │
│  ┌──────────────────┐  ┌──────────────────┐                                 │
│  │  HTML Dashboard  │  │  Artifact Upload │                                 │
│  │  (Optional)      │  │  (CI/CD)         │                                 │
│  └──────────────────┘  └──────────────────┘                                 │
│                                                                               │
└───────────────────────────────────────────────────────────────────────────────┘
```

---

## Data Flow Architecture

```
┌─────────────┐
│  Benchmark  │
│  Definition │
│             │
│  - Scenario │
│  - Targets  │
│  - OTEL cfg │
└──────┬──────┘
       │
       │ (1) Initialize
       ▼
┌──────────────────┐
│  Setup Phase     │
│                  │
│  - Create hooks  │
│  - Create stores │
│  - Init OTEL     │
│  - Warm up JIT   │
└──────┬───────────┘
       │
       │ (2) Execute
       ▼
┌──────────────────┐        ┌─────────────────────┐
│  Warmup Phase    │───────▶│  Discard Results    │
│  (10% iters)     │        └─────────────────────┘
└──────┬───────────┘
       │
       │ (3) Measure
       ▼
┌──────────────────┐        ┌─────────────────────┐
│  Measurement     │───────▶│  OTEL Spans         │
│  Phase           │        │                     │
│  (100% iters)    │        │  - benchmark.*.start│
└──────┬───────────┘        │  - hook.evaluate    │
       │                    │  - hook.result      │
       │                    │  - memory.*         │
       │                    └──────────┬──────────┘
       │                               │
       │ (4) Collect                   │
       ▼                               │
┌──────────────────┐                  │
│  Cleanup Phase   │                  │
│                  │                  │
│  - Remove hooks  │                  │
│  - Force GC      │                  │
│  - Stop OTEL     │                  │
└──────┬───────────┘                  │
       │                               │
       │ (5) Aggregate                 │
       └──────────────┬────────────────┘
                      │
                      ▼
┌──────────────────────────────────────┐
│  Span Collector                      │
│                                      │
│  const spans = exporter.getSpans()  │
│  const filtered = spans.filter(...) │
└──────────────┬───────────────────────┘
               │
               │ (6) Calculate
               ▼
┌──────────────────────────────────────┐
│  Metrics Aggregator                  │
│                                      │
│  durations = spans.map(...)          │
│  p50 = percentile(durations, 0.50)   │
│  p95 = percentile(durations, 0.95)   │
│  p99 = percentile(durations, 0.99)   │
└──────────────┬───────────────────────┘
               │
               │ (7) Validate
               ▼
┌──────────────────────────────────────┐
│  Acceptance Criteria Validator       │
│                                      │
│  passed = metrics.p95 < targets.p95  │
│  score = (passed / total) * 100      │
└──────────────┬───────────────────────┘
               │
               │ (8) Report
               ▼
┌──────────────────────────────────────┐
│  Report Generator                    │
│                                      │
│  - Console (human-readable)          │
│  - JSON (machine-readable)           │
│  - GitHub PR comment                 │
└──────────────────────────────────────┘
```

---

## Concurrent Execution Flow

```
Time ────────────────────────────────────────────────────────────────▶

┌────────────────────────────────────────────────────────────────────┐
│ Sequential Benchmark Execution (Prevents Resource Contention)     │
└────────────────────────────────────────────────────────────────────┘

┌──────────────────┐
│   Benchmark 1    │  Registration (456ms)
│   Registration   │  ████████
└──────────────────┘
                      ▼
                     (1s cool-down)
                      ▼
                    ┌──────────────────┐
                    │   Benchmark 2    │  Execution (12.3s)
                    │   Execution      │  ████████████████████████████
                    └──────────────────┘
                                         ▼
                                        (1s cool-down)
                                         ▼
                                       ┌──────────────────┐
                                       │   Benchmark 3    │  Concurrent (8.5s)
                                       │   Concurrent     │  ████████████████
                                       └──────────────────┘
                                                            ▼
                                                           ...


Within a SINGLE benchmark (e.g., Concurrent Execution):

┌────────────────────────────────────────────────────────────────────┐
│ Parallel Hook Execution Within One Benchmark                      │
└────────────────────────────────────────────────────────────────────┘

Time: 0ms                                                       8500ms
      │                                                            │
      ├─ Hook 1  ████████████▶ (85ms)                             │
      ├─ Hook 2  █████████▶ (70ms)                                │
      ├─ Hook 3  ███████████████▶ (120ms)                         │
      ├─ Hook 4  ██████▶ (50ms)                                   │
      ├─ Hook 5  ████████▶ (65ms)                                 │
      ├─ ...                                                       │
      └─ Hook 100 ███████████▶ (90ms)                             │
                                                                   │
      All hooks execute in parallel (Promise.all)               │
      Total time = max(all hook durations) ≈ 120ms              │
      Throughput = 100 hooks / 0.120s = 833 ops/sec             │
```

---

## Memory Profiling Flow

```
┌────────────────────────────────────────────────────────────────────┐
│                    Memory Profiling Timeline                       │
└────────────────────────────────────────────────────────────────────┘

Time ────────────────────────────────────────────────────────────────▶

┌──────────────┐
│  Baseline    │  GC → Measure → Record
│  Phase       │
└──────┬───────┘
       │  Baseline: { rss: 50MB, heapUsed: 20MB }
       │
       ▼
┌──────────────┐
│  Register    │  Register 1000 hooks
│  Phase       │  ████████████████████
└──────┬───────┘
       │  After Register: { rss: 150MB, heapUsed: 60MB }
       │  Delta: +100MB, +40MB
       │
       ▼
┌──────────────┐
│  Execute     │  Execute each hook 10x
│  Phase       │  ████████████████████████████████
└──────┬───────┘
       │  After Execute: { rss: 180MB, heapUsed: 70MB }
       │  Delta from register: +30MB, +10MB
       │
       ▼
┌──────────────┐
│  Cleanup     │  Clear hooks → GC → Measure
│  Phase       │
└──────┬───────┘
       │  After Cleanup: { rss: 55MB, heapUsed: 22MB }
       │
       │  Memory Leak Check:
       │    cleanup.heapUsed (22MB) vs baseline.heapUsed (20MB)
       │    Delta: +2MB (+10%)
       │    Threshold: 5%
       │    Result: ✅ PASSED (within tolerance)
       │
       ▼
┌──────────────┐
│  Report      │
│  Metrics     │
└──────────────┘
    │
    ├─ Avg memory per hook: 40MB / 1000 = 40KB ✅
    ├─ Memory leak: No ✅
    └─ Peak RSS: 180MB ✅
```

---

## CI/CD Integration Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                      GitHub Actions Workflow                        │
└─────────────────────────────────────────────────────────────────────┘

Trigger: PR opened/updated, push to main, nightly schedule
         │
         ▼
┌────────────────────┐
│  Checkout Code     │
└────────┬───────────┘
         │
         ▼
┌────────────────────┐
│  Setup Environment │
│  - Node.js 20      │
│  - pnpm install    │
└────────┬───────────┘
         │
         ▼
┌────────────────────┐
│  Run Benchmarks    │
│  node --expose-gc  │
│  benchmarks/       │
│  runner.mjs        │
└────────┬───────────┘
         │
         ├─────────────────┐
         │                 │
         ▼                 ▼
┌────────────────┐  ┌──────────────────┐
│ Upload JSON    │  │ Compare Baseline │
│ Results        │  │ (if exists)      │
└────────┬───────┘  └────────┬─────────┘
         │                   │
         │                   ├──── Regression detected?
         │                   │     YES ──▶ ❌ Fail CI
         │                   │     NO  ──▶ ✅ Continue
         │                   │
         ├───────────────────┘
         │
         ▼
┌────────────────────┐
│  Post PR Comment   │  (if PR event)
│                    │
│  📊 Benchmark Results:
│  ✅ All 6 benchmarks passed
│  ✅ No regressions detected
│
│  Critical Metrics:
│  - Hook Exec p95: 34.2ms (✅ < 50ms)
│  - Throughput: 1234 ops/s (✅ > 1000)
│  - Memory/hook: 4.8MB (✅ < 5MB)
│
│  View full report: [link]
└────────────────────┘
         │
         ▼
┌────────────────────┐
│  Store Baseline    │  (if main branch)
│  (for future       │
│   comparisons)     │
└────────────────────┘
```

---

## File Structure Diagram

```
unrdf/
├── benchmarks/
│   ├── 01-hook-registration.bench.mjs          ◀─┐
│   ├── 02-hook-execution-latency.bench.mjs       │ 6 Core Benchmarks
│   ├── 03-concurrent-execution.bench.mjs         │ (80/20 Coverage)
│   ├── 04-memory-footprint.bench.mjs             │
│   ├── 05-condition-evaluation.bench.mjs         │
│   ├── 06-circuit-breaker.bench.mjs            ◀─┘
│   │
│   ├── fixtures/                                ◀─┐
│   │   ├── test-hooks.mjs                         │ Reusable Test Data
│   │   ├── test-events.mjs                        │
│   │   └── test-stores.mjs                      ◀─┘
│   │
│   ├── utils/                                    ◀─┐
│   │   ├── otel-collector.mjs                      │ Instrumentation
│   │   ├── metrics-aggregator.mjs                  │ & Analysis Tools
│   │   ├── percentile-calculator.mjs               │
│   │   └── memory-profiler.mjs                   ◀─┘
│   │
│   ├── runner.mjs                                ◀─── Main Entry Point
│   ├── compare-baseline.mjs                      ◀─── Regression Detector
│   └── baselines/                                ◀─── Historical Baselines
│       ├── baseline-2025-12-04.json
│       └── baseline-latest.json
│
├── docs/
│   ├── benchmark-architecture-80-20.md           ◀─── This Document
│   └── benchmark-architecture-diagram.md         ◀─── Visual Diagrams
│
├── .github/
│   └── workflows/
│       └── benchmarks.yml                        ◀─── CI/CD Integration
│
└── src/
    └── knowledge-engine/
        ├── knowledge-hook-manager.mjs            ◀─┐
        ├── hook-executor.mjs                       │ System Under Test
        ├── condition-evaluator.mjs                 │
        └── observability.mjs                     ◀─┘
```

---

## OTEL Span Hierarchy

```
benchmark.hook-registration.start [Suite Span]
├── benchmark.hook-registration.warmup
│   ├── hook.register [Warmup iteration 1]
│   ├── hook.register [Warmup iteration 2]
│   └── ...
├── benchmark.hook-registration.iteration [Measurement iteration 1]
│   └── hook.register
│       ├── hook.validate
│       ├── hook.security-check
│       └── hook.store
├── benchmark.hook-registration.iteration [Measurement iteration 2]
│   └── hook.register
│       └── ...
├── benchmark.hook-registration.iteration [...]
├── benchmark.hook-registration.aggregate
│   ├── metrics.calculate-percentiles
│   └── metrics.calculate-throughput
├── benchmark.hook-registration.validate
│   ├── criteria.check-p95
│   ├── criteria.check-memory
│   └── criteria.check-throughput
└── benchmark.hook-registration.end

Attributes on benchmark.hook-registration.iteration:
{
  'benchmark.suite': 'hook-registration',
  'benchmark.iteration': 42,
  'benchmark.duration_ms': 7.2,
  'benchmark.memory_delta_bytes': 85000,
  'benchmark.success': true,
}

Attributes on benchmark.hook-registration.validate:
{
  'validation.total_criteria': 5,
  'validation.passed': 5,
  'validation.failed': 0,
  'validation.score': 100,
}
```

---

## Critical Path Analysis

```
What matters most for production performance?
(Pareto analysis - 80/20 rule)

┌────────────────────────────────────────────────────────────────┐
│                  Performance Impact Distribution               │
└────────────────────────────────────────────────────────────────┘

Impact on Production Performance:

█████████████████████████████████████████ 40%  Hook Execution Latency
                                                (p95 < 50ms target)
                                                → Benchmark 2

████████████████████ 20%  Hook Registration Time
                          (< 10ms target)
                          → Benchmark 1

███████████████ 15%  Concurrent Throughput
                      (> 1000 ops/s target)
                      → Benchmark 3

███████████████ 15%  Memory Per Hook
                      (< 5MB target)
                      → Benchmark 4

██████ 5%  Circuit Breaker Reliability
           (< 1% trip rate)
           → Benchmark 6

██████ 5%  Condition Cache Hit Rate
           (> 80% hit rate)
           → Benchmark 5

────────────────────────────────────────────────────────────────
100%   Total Impact Covered by 6 Benchmarks

Non-critical metrics EXCLUDED (vanity metrics):
- Code coverage of benchmarks
- Number of assertions
- Test file count
- Documentation coverage
- Hook definition complexity
- SPARQL query line count
```

---

**End of Visual Diagrams Document**
