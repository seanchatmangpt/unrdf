# Version & Environment — UNRDF

- Workspace version: latest (all packages unified)
- `.tool-versions` overrides volta settings (nodejs latest, pnpm latest)
- When version info is needed, always read `package.json` — never hardcode
