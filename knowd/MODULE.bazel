# knowd Bazel module configuration

module(
    name = "knowd",
    version = "0.1.0",
)

# Use Go toolchain
bazel_dep(name = "rules_go", version = "0.40.0")

go = use_extension("@rules_go//go:extensions.bzl", "go_sdk")
use_repo(go)