#!/usr/bin/env sh
# UNRDF Pre-push Quality Gate
# Runs fast test suite before pushing

echo "Running pre-push tests..."
START=$(date +%s)
pnpm test:fast || exit 1
END=$(date +%s)
echo "Pre-push tests passed ($(($END - $START))s)"
