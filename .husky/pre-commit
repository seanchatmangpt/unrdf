#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# UNRDF Pre-commit Quality Gate
# Enforces code quality standards on changed files only (80/20 focus)

echo "🔍 Running pre-commit quality checks..."

# Run lint-staged for incremental checks on changed files
pnpm lint-staged

# Exit with lint-staged's exit code
exit $?
