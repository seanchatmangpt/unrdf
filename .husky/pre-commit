#!/usr/bin/env sh

# UNRDF Pre-commit Quality Gate
# Lints only staged files via lint-staged (~2-3s)
# Tests run on pre-push (see .husky/pre-push)

echo "Running pre-commit lint..."
pnpm lint-staged || exit 1
echo "Pre-commit passed"
